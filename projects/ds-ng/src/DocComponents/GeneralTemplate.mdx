import {
  Controls,
  Description,
  Meta,
  Primary,
  Stories,
  Title,
} from "@storybook/addon-docs/blocks";

import { TitleOfControls } from "./Utils.mdx";
import { STORIES_TITLE } from "../lib/utils/doc/utils";

export const GeneralTemplate = ({children, showStories = true}) => (
  <section>
    {children && <Meta of={children} />}
    {children && <Title of={children}>{children?.title}</Title> || <Title />}
    {children && <Description of={children}>{children}</Description> || <Description />}
    {children && <Primary of={children}/> || <Primary />}
    <TitleOfControls />
    {children && <Controls of={children.Default} /> || <Controls />}
    {showStories && <Stories title={STORIES_TITLE} includePrimary={false} />}
  </section>
);
