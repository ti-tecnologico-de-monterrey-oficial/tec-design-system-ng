import { useState } from "react";

export const Accordion = ({ elements = [] }) => {
  return (
    <section className="bmb_doc-accordion--container">
      {elements.map((element, index) => (
        <details key={index} className="bmb_doc-accordion--item">
          <summary>{element?.title}</summary>
          <div className="bmb_doc-accordion--content">{element?.content}</div>
        </details>
      ))}
    </section>
  );
};
