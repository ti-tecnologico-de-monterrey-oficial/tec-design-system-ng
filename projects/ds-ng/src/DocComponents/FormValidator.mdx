import {
  Controls,
  Description,
  Meta,
  Stories,
  Title,
} from "@storybook/addon-docs/blocks";

import * as formStory from "../lib/components/bmb-form-validator/bmb-form-validator.stories";
import * as example from "../lib/components/bmb-form-validator/bmb-form-validator-example.stories";
import { TitleOfControls } from "./Utils.mdx";
import { STORIES_TITLE } from "../lib/utils/doc/utils";

export const FormValidator = () => (
  <section>
    <Meta of={formStory} />
    <Title of={formStory}>{formStory?.title}</Title>
    <Description of={formStory} />
    <TitleOfControls />
    <Controls of={formStory.Default} />
    <Meta of={example} />
    <Stories title={STORIES_TITLE} includePrimary={false} showName={true} />
  </section>
);
