import { Meta } from '@storybook/addon-docs/blocks';

import * as defaultStory from './bmb-item-default/bmb-item-default.stories';

import { ListTemplate } from '../../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../../utils/doc/utils';

<Meta title="Dev tools/Item" />
<ListTemplate
  content={[
    defaultStory,

]}
context={{
    [getFormatName(
      defaultStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: true,
    },
  }}

/>
