import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from './bmb-table-lite.stories';
import * as subStory from './bmb-table-lite-server.stories';
import { ListTemplate } from '../../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../../utils/doc/utils';

<Meta of={mainStory} />
<ListTemplate
  main={mainStory}
  content={[subStory]}
  context={{
    [getFormatName(
      subStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
  }}
/>
