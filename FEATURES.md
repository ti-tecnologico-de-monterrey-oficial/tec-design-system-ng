# Novedades ## [v1.6.2] -
2026-04-09

### 🚀 Novedades

*
fix(component / dropdown menu): Ahora solo se cierra a si mismo cuando cierra el modal
*
fix(component / image): Se mejoró el comportamiento responsivo del componente Imagen en combinación con el multi dot paginator
*
feat(component / profile): El componente de profile ahora admite el cambio de role y emite el evento getRoleSwitch
*
Formato
*
fix(component/home-card): se agregaron estilos (CSS) para todos los componentes y elementos que usan el 'Home card' para mostrar la sombra cuando se implemente mediante el 'Bmb content projection service'
*
notification card
*
profile updated
*
feat(component / Interactive icon): Se agregó el color blanco como opción de fondo
*
fix(component/dropdown): se modificó la validación de tamaño de archivo para mostrar el mensaje de error correcto.
*
chore: se eliminó código innecesario.
*
chore: se eliminó código innecesario.

### 🆕 Nuevos Componentes

- **fix(component / dropdown menu): Ahora solo se cierra a si mismo cuando cierra el modal**:
fix(component / dropdown menu): Ahora solo se cierra a si mismo cuando cierra el modal
- **fix(component / image): Se mejoró el comportamiento responsivo del componente Imagen en combinación con el multi dot paginator**:
fix(component / image): Se mejoró el comportamiento responsivo del componente Imagen en combinación con el multi dot paginator
- **feat(component / profile): El componente de profile ahora admite el cambio de role y emite el evento getRoleSwitch**:
feat(component / profile): El componente de profile ahora admite el cambio de role y emite el evento getRoleSwitch
- **Formato**:
Formato
- **fix(component/home-card): se agregaron estilos (CSS) para todos los componentes y elementos que usan el 'Home card' para mostrar la sombra cuando se implemente mediante el 'Bmb content projection service'**:
fix(component/home-card): se agregaron estilos (CSS) para todos los componentes y elementos que usan el 'Home card' para mostrar la sombra cuando se implemente mediante el 'Bmb content projection service'
- **notification card**:
notification card
- **profile updated**:
profile updated
- **feat(component / Interactive icon): Se agregó el color blanco como opción de fondo**:
feat(component / Interactive icon): Se agregó el color blanco como opción de fondo
- **fix(component/dropdown): se modificó la validación de tamaño de archivo para mostrar el mensaje de error correcto.**:
fix(component/dropdown): se modificó la validación de tamaño de archivo para mostrar el mensaje de error correcto.
- **chore: se eliminó código innecesario.**:
chore: se eliminó código innecesario.
- **chore: se eliminó código innecesario.**:
chore: se eliminó código innecesario.

### 🔄 Componentes Actualizados

- **fix(component / dropdown menu): Ahora solo se cierra a si mismo cuando cierra el modal**:
fix(component / dropdown menu): Ahora solo se cierra a si mismo cuando cierra el modal
- **fix(component / image): Se mejoró el comportamiento responsivo del componente Imagen en combinación con el multi dot paginator**:
fix(component / image): Se mejoró el comportamiento responsivo del componente Imagen en combinación con el multi dot paginator
- **feat(component / profile): El componente de profile ahora admite el cambio de role y emite el evento getRoleSwitch**:
feat(component / profile): El componente de profile ahora admite el cambio de role y emite el evento getRoleSwitch
- **Formato**:
Formato
- **fix(component/home-card): se agregaron estilos (CSS) para todos los componentes y elementos que usan el 'Home card' para mostrar la sombra cuando se implemente mediante el 'Bmb content projection service'**:
fix(component/home-card): se agregaron estilos (CSS) para todos los componentes y elementos que usan el 'Home card' para mostrar la sombra cuando se implemente mediante el 'Bmb content projection service'
- **notification card**:
notification card
- **profile updated**:
profile updated
- **feat(component / Interactive icon): Se agregó el color blanco como opción de fondo**:
feat(component / Interactive icon): Se agregó el color blanco como opción de fondo
- **fix(component/dropdown): se modificó la validación de tamaño de archivo para mostrar el mensaje de error correcto.**:
fix(component/dropdown): se modificó la validación de tamaño de archivo para mostrar el mensaje de error correcto.
- **chore: se eliminó código innecesario.**:
chore: se eliminó código innecesario.
- **chore: se eliminó código innecesario.**:
chore: se eliminó código innecesario.

---
