import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from './bmb-accordion.stories';
import * as simpleTextStory from './bmb-accordion-simple-text/bmb-accordion-simple-text.stories';
import * as secondStory from '../../directives/old/bmb-accordion/bmb-accordion-control.stories';
import { ListTemplate } from '@docs/components/ListTemplate.mdx';
import { getFormatName } from '../../../../../docs/utils/utils';

<Meta of={mainStory} />
<ListTemplate
  main={mainStory}
  content={[simpleTextStory, secondStory]}
  context={{
    [getFormatName(
      simpleTextStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
    [getFormatName(
      secondStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
  }}
/>
