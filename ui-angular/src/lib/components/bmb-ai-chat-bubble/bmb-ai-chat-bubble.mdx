import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from './bmb-ai-chat-bubble.stories';
import * as editorStory from './bmb-ai-chat-bubble-editor/bmb-ai-chat-bubble-editor.stories';
import { ListTemplate } from '@docs/components/ListTemplate.mdx';
import { getFormatName } from '@docs/utils/utils';

<Meta of={mainStory} />
<ListTemplate
  content={[mainStory, editorStory]}
  context={{
    [getFormatName(
      editorStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: true,
    },
  }}
/>
