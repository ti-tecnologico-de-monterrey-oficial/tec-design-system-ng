import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from './bmb-card.stories';
import * as informativeStory from './templates/bmb-card-informative.stories';
import * as flatStory from './templates/bmb-card-flat.stories';
import * as actionsStory from './templates/bmb-card-actions.stories';
import * as homeStory from './templates/bmb-card-home.stories';
import * as emptyStory from './templates/bmb-card-empty.stories';
import { ListTemplate } from '@docs/components/ListTemplate.mdx';
import { getFormatName } from '@docs/utils/utils';

<Meta title="Components/Containers/Generic card" />
<ListTemplate
  content={[
    mainStory,
    informativeStory,
    flatStory,
    actionsStory,
    homeStory,
    emptyStory,
  ]}
  context={{
    [getFormatName(
      mainStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: true,
    },
    [getFormatName(
      informativeStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: false,
    },
    [getFormatName(
      flatStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: false,
    },
    [getFormatName(
      actionsStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: false,
    },
    [getFormatName(
      homeStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: false,
    },
    [getFormatName(
      emptyStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: true,
      showControls: false,
    },
  }}
/>
