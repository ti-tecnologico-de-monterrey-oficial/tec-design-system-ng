import { Meta } from '@storybook/addon-docs/blocks';

import * as defaultStory from './bmb-item-default/bmb-item-default.stories';
import * as hyperlinkStory from './bmb-item-hyperlink/bmb-item-hyperlink.stories';
import * as informativeTextStory from './bmb-item-informative-text/bmb-item-informative-text.stories';
import * as itemActionStory from './bmb-item-actions/bmb-item-actions.stories';

import { ListTemplate } from '../../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../../utils/doc/utils';

<Meta title="Dev tools/Item" />
<ListTemplate
  content={[
    defaultStory,
    hyperlinkStory,
    informativeTextStory,
    itemActionStory,
]}
context={{
    [getFormatName(
      defaultStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: true,
    },
    [getFormatName(
      hyperlinkStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: true,
    },
    [getFormatName(
      informativeTextStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: true,
    },
    [getFormatName(
      itemActionStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: true,
    }
  }}

/>
