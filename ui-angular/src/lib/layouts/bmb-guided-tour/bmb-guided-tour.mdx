import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from './bmb-guided-tour.stories';
import * as variantStory from './bmb-guided-tour-variant.stories';
import { ListTemplate } from '../../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../../utils/doc/utils';

<Meta title="Templates/Guided tour" />
<ListTemplate
  content={[variantStory, mainStory]}
  context={{
    [getFormatName(
      mainStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
  }}
/>
