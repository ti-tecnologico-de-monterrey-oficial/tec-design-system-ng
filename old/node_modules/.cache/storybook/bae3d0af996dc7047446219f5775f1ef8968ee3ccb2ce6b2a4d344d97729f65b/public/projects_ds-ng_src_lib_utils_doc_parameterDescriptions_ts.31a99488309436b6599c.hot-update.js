"use strict";
self["webpackHotUpdate_ti_tecnologico_de_monterrey_oficial_tec_design_system_ng"]("projects_ds-ng_src_lib_utils_doc_parameterDescriptions_ts",{

/***/ "./projects/ds-ng/src/lib/utils/doc/parameterDescriptions.ts"
/*!*******************************************************************!*\
  !*** ./projects/ds-ng/src/lib/utils/doc/parameterDescriptions.ts ***!
  \*******************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   DBmbButtonParamDesc: () => (/* binding */ DBmbButtonParamDesc),
/* harmony export */   DBmbDropdownMenuParamDesc: () => (/* binding */ DBmbDropdownMenuParamDesc),
/* harmony export */   DBmbGenericParamDesc: () => (/* binding */ DBmbGenericParamDesc),
/* harmony export */   DBmbHomeCardParamDesc: () => (/* binding */ DBmbHomeCardParamDesc),
/* harmony export */   DBmbIconParamDesc: () => (/* binding */ DBmbIconParamDesc),
/* harmony export */   DBmbImageParamDesc: () => (/* binding */ DBmbImageParamDesc),
/* harmony export */   DBmbInputParamDesc: () => (/* binding */ DBmbInputParamDesc),
/* harmony export */   DBmbLayoutParamDesc: () => (/* binding */ DBmbLayoutParamDesc),
/* harmony export */   DBmbModalParamDesc: () => (/* binding */ DBmbModalParamDesc),
/* harmony export */   DBmbProgressBar: () => (/* binding */ DBmbProgressBar),
/* harmony export */   DBmbStepProgressBar: () => (/* binding */ DBmbStepProgressBar),
/* harmony export */   DBmbTooltipParamDesc: () => (/* binding */ DBmbTooltipParamDesc),
/* harmony export */   DEFAULT_VALUE_DESC: () => (/* binding */ DEFAULT_VALUE_DESC),
/* harmony export */   DEFAULT_VALUE_DETAIL: () => (/* binding */ DEFAULT_VALUE_DETAIL),
/* harmony export */   GOOGLE_FONTS_LINK: () => (/* binding */ GOOGLE_FONTS_LINK),
/* harmony export */   ICON_DESCRIPTION: () => (/* binding */ ICON_DESCRIPTION),
/* harmony export */   ICON_IMAGE_DETAIL: () => (/* binding */ ICON_IMAGE_DETAIL),
/* harmony export */   ON_BUTTON_CLICK: () => (/* binding */ ON_BUTTON_CLICK),
/* harmony export */   ON_CLICK_DESCRIPTION: () => (/* binding */ ON_CLICK_DESCRIPTION),
/* harmony export */   SIMPLE_ICON_DESCRIPTION: () => (/* binding */ SIMPLE_ICON_DESCRIPTION),
/* harmony export */   getAppearanceDescription: () => (/* binding */ getAppearanceDescription),
/* harmony export */   getAppearanceParam: () => (/* binding */ getAppearanceParam),
/* harmony export */   getControlDescription: () => (/* binding */ getControlDescription),
/* harmony export */   getDefaultValueControl: () => (/* binding */ getDefaultValueControl),
/* harmony export */   getDefaultValueDesc: () => (/* binding */ getDefaultValueDesc),
/* harmony export */   getFormControlConsiderations: () => (/* binding */ getFormControlConsiderations),
/* harmony export */   getFormControlDescription: () => (/* binding */ getFormControlDescription),
/* harmony export */   getLabelDescription: () => (/* binding */ getLabelDescription),
/* harmony export */   getLabelParamDesc: () => (/* binding */ getLabelParamDesc),
/* harmony export */   getOnClickParam: () => (/* binding */ getOnClickParam),
/* harmony export */   getOnEventParam: () => (/* binding */ getOnEventParam),
/* harmony export */   getPropertyForType: () => (/* binding */ getPropertyForType),
/* harmony export */   getPropertyParamDesc: () => (/* binding */ getPropertyParamDesc),
/* harmony export */   getWidthIncreaseDesc: () => (/* binding */ getWidthIncreaseDesc)
/* harmony export */ });
/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils */ "./projects/ds-ng/src/lib/utils/doc/utils.ts");

const DISABLE_DESCRIPTION = `
Disables the element when true, making it non-interactive and cannot be clicked.

This is useful for conditions where user interaction should be restricted.
`;
const UNIQUE_IDENTIFY_DESCRIPTION = 'Sets the unique identifier for';
const LINK_DESCRIPTION = 'Sets the link for redirection to another or same page.';
const ON_CLICK_DESCRIPTION = ', this event is only emitted if the *link* property is empty';
const GOOGLE_FONTS_LINK = `Please refer to [Google Fonts](https://fonts.google.com/icons?icon.size=24&icon.color=%23e8eaed&selected=Material+Symbols+Outlined:more_vert:FILL@0;wght@400;GRAD@0;opsz@24) for more icons.`;
const ICON_IMAGE_DETAIL = `${(0,_utils__WEBPACK_IMPORTED_MODULE_0__.getAlertBlockquote)(`Images are also supported; instead of the icon name, enter the URL or path of the image to use.`, {
  title: '###'.concat(_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note),
  blockquoteType: _utils__WEBPACK_IMPORTED_MODULE_0__.BlockquoteType.note
})}
<br/>
${(0,_utils__WEBPACK_IMPORTED_MODULE_0__.getAlertBlockquote)(`Please remember that images need an *alt* to be accessible.
Look for the property to set the alt, if it does not exist in the component it is because the component implements it internally.
`, {
  title: '###'.concat(_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.warning),
  blockquoteType: _utils__WEBPACK_IMPORTED_MODULE_0__.BlockquoteType.warning
})}`;
const SIMPLE_ICON_DESCRIPTION = `Sets the icon name that will be shown.
<br/><br/>${GOOGLE_FONTS_LINK}`;
const ICON_DESCRIPTION = `${SIMPLE_ICON_DESCRIPTION}<br/><br/>${ICON_IMAGE_DETAIL.replaceAll('###', '')}`;
const DEFAULT_VALUE_DESC = `It is not necessary to explicitly set default values, the property can be omitted.<br/><br/>
  Properties can be omitted if they are not required and do not contain a default value.`;
const DEFAULT_VALUE_DETAIL = `${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note.replace(/(<br\/>)|(\*)/g, '')}

${DEFAULT_VALUE_DESC.replace(/(<br\/>)|(\*)/g, '')}`;
const ON_BUTTON_CLICK = (0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'buttonClick');
const getWidthIncreaseDesc = name => `${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note}
The width of the ${name} will increase depending on the length of the text.`;
const getDefaultValueDesc = defaultValue => `
${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note}
The default value is ${defaultValue}.${DEFAULT_VALUE_DESC}`;
const getDefaultValueControl = (summary = '""') => {
  const _summary = typeof summary === 'string' ? summary === '' ? '""' : summary : summary.toString();
  const isLong = _summary.length > 10;
  return {
    summary: isLong ? _summary.substring(0, 10).concat('...') : _summary,
    detail: (isLong ? 'Default value: '.concat(_summary).concat(`

`) : '').concat(DEFAULT_VALUE_DETAIL)
  };
};
const getLabelDescription = (positionDescription, type = ' field') => `
Sets the label associated with ${type}.

The label helps users understand the context of what the ${type} represents.

The label will be displayed ${positionDescription} the ${type}.
`;
const getCheckboxOrRadialLabel = type => {
  return {
    control: {
      type: 'text'
    },
    description: `
${getLabelDescription('at the default `labelPosition` or the position specified in `labelPosition` relative to', type)}
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  };
};
const getLabelPosition = type => {
  return {
    control: {
      type: 'radio'
    },
    options: ['before', 'after'],
    description: `
Specifies the position of the label relative to ${type}.

The possible positions to indicate where the label should be displayed in relation to the ${type} are:

- before
- after
      `,
    table: {
      category: 'Properties',
      type: {
        summary: 'IBbmSidePosition',
        detail: `IBbmSidePosition = 'before' | 'after'`
      },
      defaultValue: getDefaultValueControl('after')
    }
  };
};
const getFormControlConsiderations = (replaceChar = '') => `__
__It is essential to assign the property \`name\` for correct behavior of the field.
__
__It will not be necessary to define \`Validators\` in the \`FormControl\` instance, as long as the properties of this field are correctly assigned.
__
__
__This component automatically implements the \`Validators\` on the following properties:
__
__- **isRequired**: adds \`Validators.required\` to the \`FormControl\`
__- **minlength**: adds \`Validators.minLength\` to the \`FormControl\`
__- **maxlength**: adds \`Validators.maxLength\` to the \`FormControl\`
__- **max**: adds \`Validators.max\` to the \`FormControl\`
__- **min**: adds \`Validators.min\` to the \`FormControl\`
__- **pattern**: adds \`Validators.pattern\` to the \`FormControl\``.replaceAll('__', replaceChar);
const getFormControlDescription = (replaceChar = '') => `__In ${_utils__WEBPACK_IMPORTED_MODULE_0__.DESIGN_SYSTEM_TITLE}, it is possible to implement automatic field validation using the \`bmb-form-validator\` component.
__
__<br/>
__The \`bmb-form-validator\` component contains the state of the form by collecting the form fields and adding them to a \`FormGroup\`.`.replaceAll('__', replaceChar);
const getControlDescription = (isComplete = false) => `
Sets the \`FormControl\` instance to manage the state of the field.
${isComplete ? `

<br/>
${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.important}
${getFormControlConsiderations()}
<br/>
${getFormControlDescription()}

<br/>
Please check the ***Form validator*** documentation for details on how to implement \`bmb-form-validator\` component.

<br/>
This documentation is displayed as a tab at the top.
` : ''}
`;
const getPropertyParamDesc = (name, controlType = 'text', defaultSummary = '""', additionalDescription = '', alternativeDescription = '', alternativePropName = '', summaryType = '') => {
  return {
    control: !!controlType && {
      type: controlType
    },
    description: alternativeDescription || (controlType === 'boolean' ? `Sets the ${name} when true.${additionalDescription}` : '') || `Sets the ${alternativePropName || 'title'} of ${name}.${additionalDescription}`,
    table: {
      category: 'Properties',
      defaultValue: controlType !== 'boolean' && defaultSummary === false || controlType && getDefaultValueControl(defaultSummary),
      type: {
        summary: summaryType || (controlType === 'text' ? 'string' : controlType)
      }
    }
  };
};
const getLabelParamDesc = (position = '', name = '', additionalDescription = '') => getPropertyParamDesc('', 'text', '', '', `Sets the ${name || 'label'} for the content${!!position ? ` of the ${position} section` : ''}${additionalDescription}.`);
const getAppearanceDescription = name => `Sets the appearance of ${name}, affecting its visual style.`;
const getAppearanceParam = (name, options, defaultSummary = '', additionalDescription = '') => {
  const detailType = options.toString().replaceAll(',', `
`);
  return {
    control: {
      type: 'select'
    },
    options,
    description: `${getAppearanceDescription(name)}${additionalDescription}`,
    table: {
      category: 'Properties',
      defaultValue: defaultSummary === '' ? {
        summary: ''
      } : getDefaultValueControl(defaultSummary),
      type: {
        summary: 'string',
        detail: detailType
      }
    }
  };
};
const getOnEventParam = (onEvent, additionalDescription = '', eventType = 'change') => {
  return {
    control: false,
    description: `Emits the event ${eventType === 'change' || eventType === 'other' ? `${eventType === 'change' ? `when ${onEvent.name} changed.` : ''}` : 'when a key is pressed while the input is focused.'}${additionalDescription}`,
    table: {
      category: 'Events',
      defaultValue: false,
      type: {
        summary: onEvent.propertyValue,
        detail: onEvent.event_type
      }
    }
  };
};
const getOnClickParam = (onEvent, additionalDescription = '', buttonEventType = 'clicked') => getOnEventParam(onEvent, `triggered after ${onEvent.name} button is ${buttonEventType}${additionalDescription}.`, 'other');
const DBmbGenericParamDesc = {
  uniqueId: {
    control: {
      type: 'text'
    },
    description: `

${UNIQUE_IDENTIFY_DESCRIPTION} the field.

This property is used to link the label to the field through the ***for*** attribute, improving accessibility and usability.
    `,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  disabled: {
    control: {
      type: 'boolean'
    },
    description: DISABLE_DESCRIPTION,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('false'),
      type: {
        summary: 'boolean'
      }
    }
  },
  actionHeaders: {
    control: {
      type: 'object'
    },
    description: `
Sets the elements to execute actions or page navigation.

These are to implement icons as buttons or links according to the needs of the application, it contains toggle functionalities to indicate different actions.
      `,
    table: {
      category: 'Properties',
      defaultValue: {
        summary: '[]',
        detail: `//Example
[
  {
    icon: 'home',
    link: 'https://www.example.com/',
  },
  {
    icon: 'share',
    action: () => {
      console.log('share');
    },
  },
  {
    icon: 'inventory_2',
    link: 'https://www.example.com/',
    target: '_blank',
  },
  {
    icon: 'send',
    action: () => {
      console.log('send');
    },
  },
]`
      },
      type: {
        summary: 'IBmbActionHeader[] (optional)',
        detail: `
IBmbActionHeader {
  icon: string;
  alt?: string;
  iconSize?: number;
  iconActiveToggle?: string;
  isToggleActive?: boolean;
  isAccentColor?: boolean;
  link?: string;
  target?: IBmbTargetLink;
  action: () => void;
}

IBmbTargetLink = '_blank' | '_parent' | '_self' | '_top';
          `
      }
    }
  },
  link: {
    control: {
      type: 'text'
    },
    description: `${LINK_DESCRIPTION}`,
    table: {
      category: 'Events',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  linkOrButton: {
    control: {
      type: 'text'
    },
    description: `${LINK_DESCRIPTION} If this property is empty it will emit the button event.`,
    table: {
      category: 'Events',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  target: {
    control: {
      type: 'radio'
    },
    options: ['_blank', '_self', '_parent', '_top'],
    description: 'Sets the target property for the link. Refer to [here](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a) for more information.',
    table: {
      category: 'Events',
      type: {
        summary: 'IBmbTargetLink (option)'
      },
      defaultValue: getDefaultValueControl('_blank')
    }
  },
  lang: {
    control: {
      type: 'text'
    },
    description: `
Sets the language for all text, numbers, currencies, date formats, etc.

By default, the supported language are:
- ***en***: english
- ***es***: spanish.
      `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'es'
      }
    }
  },
  deprecated: {
    control: false,
    description: (0,_utils__WEBPACK_IMPORTED_MODULE_0__.getDeprecatedDesc)({
      type: 'property'
    }),
    table: {
      category: 'Deprecated',
      defaultValue: false
    }
  },
  onButtonClick: getOnClickParam(ON_BUTTON_CLICK, ON_CLICK_DESCRIPTION),
  onButtonPress: getOnClickParam((0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'buttonPress'), ON_CLICK_DESCRIPTION, 'pressed'),
  onButtonKeyPress: getOnClickParam((0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'buttonKeyPress'), ' with a keyboard'.concat(ON_CLICK_DESCRIPTION), 'pressed')
};
const DBmbImageParamDesc = {
  image: {
    control: {
      type: 'text'
    },
    description: 'Sets the source of the image to display, either from a path or a URL.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  src: {
    control: {
      type: 'text'
    },
    description: 'Sets the Hi-res image source URL',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  mobileSrc: {
    control: {
      type: 'text'
    },
    description: 'Sets the low-res image source URL',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  alt: {
    control: {
      type: 'text'
    },
    description: `
Sets the alternative text of the images. It is an essential part of making content accessible.

Refer [here](https://www.w3.org/WAI/alt/) for more information.
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  width: {
    control: {
      type: 'text'
    },
    description: 'Sets a blurred image as a box shadow.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl('100%')
    }
  },
  ratio: {
    control: {
      type: 'text'
    },
    description: 'Sets the aspect ratio of the image.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  borderRadius: {
    control: {
      type: 'select'
    },
    options: ['xs', 's', 'm', 'l', 'xl', 'none'],
    table: {
      type: {
        summary: 'string'
      },
      category: 'Properties',
      defaultValue: getDefaultValueControl('m')
    },
    description: 'Sets the corner radius size.'
  },
  loading: {
    control: {
      type: 'radio'
    },
    options: ['lazy', 'eager'],
    table: {
      type: {
        summary: 'string'
      },
      category: 'Properties',
      defaultValue: getDefaultValueControl('lazy')
    },
    description: `
Enables the loading behavior.
- **eager**: the browser will load the image immediately.
- **lazy**: the browser will wait until the viewport is close to the image to load it.
        `
  },
  enableZoom: {
    control: {
      type: 'boolean'
    },
    description: `
Sets the zoom configuration when true.<br/><br/>
It groups multiple interactive icons into a parent element.<br/><br/>
By default, it is false, and you do not need to explicitly set it.
The badge should always have a parent element.`,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('false'),
      type: {
        summary: 'boolean'
      }
    }
  },
  isBlurredBackdrop: {
    control: {
      type: 'boolean'
    },
    description: 'Sets a blurred image as box shadow.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('false'),
      type: {
        summary: 'boolean'
      }
    }
  }
};
const DBmbIconParamDesc = {
  icon: {
    control: {
      type: 'text'
    },
    description: ICON_DESCRIPTION,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  iconSize: {
    control: {
      type: 'number'
    },
    description: `Sets the size of the icon or width of the image to use.<br/><br/>
${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note} <= 0 will be inherited.`,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'number'
      }
    }
  },
  isIconFill: {
    control: {
      type: 'boolean'
    },
    description: 'Determines whether the icon is filled (***true***) or outlined (***false***).',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('true'),
      type: {
        summary: 'boolean'
      }
    }
  },
  iconDotNotification: {
    control: {
      type: 'number'
    },
    description: 'Sets the notification counter number (0 to hide).',
    table: {
      category: 'Properties',
      defaultValue: false,
      type: {
        summary: 'number'
      }
    }
  },
  alt: {
    ...DBmbImageParamDesc.alt,
    description: DBmbImageParamDesc.alt.description.replace('of the images', 'for the icon when it is an image')
  }
};
const DBmbButtonParamDesc = {
  appearance: {
    control: {
      type: 'select'
    },
    options: ['primary', 'secondary-filled', 'secondary-outlined', 'destructive', 'transparent'],
    description: getAppearanceDescription('the buttons'),
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('primary'),
      type: {
        summary: 'string'
      }
    }
  },
  size: {
    control: 'radio',
    options: ['small', 'large'],
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('small'),
      type: {
        summary: 'string'
      }
    },
    description: 'Sets the size of the button, affecting its visual size.'
  },
  icon: {
    control: {
      type: 'text'
    },
    description: ICON_DESCRIPTION.concat('<br/><br/>The icon to display on the button.'),
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  }
};
const DBmbModalParamDesc = {
  title: {
    control: {
      type: 'text'
    },
    description: 'Specifies the text display. This message should be concise and direct.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  primaryBtnLabel: {
    control: {
      type: 'text'
    },
    description: 'Specifies the text of the primary button.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  },
  secondaryBtnLabel: {
    control: {
      type: 'text'
    },
    description: 'Specifies the text of the secondary button.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string'
      }
    }
  }
};
const DBmbHomeCardParamDesc = {
  icon: {
    ...DBmbIconParamDesc.icon,
    table: {
      ...DBmbIconParamDesc.icon.table,
      type: {
        ...DBmbIconParamDesc.icon.table.type,
        summary: DBmbIconParamDesc.icon.table.type.summary.concat(' (optional)')
      }
    }
  },
  iconSize: {
    ...DBmbIconParamDesc.iconSize,
    table: {
      ...DBmbIconParamDesc.iconSize.table,
      type: {
        ...DBmbIconParamDesc.iconSize.table.type,
        summary: DBmbIconParamDesc.iconSize.table.type.summary.concat(' (optional)')
      }
    }
  },
  bgIconAppearance: {
    control: {
      type: 'text'
    },
    description: 'Sets icon background color.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'IBmbColor (optional)',
        detail: `IBmbColor =
  | 'blue-mariner-50'
  | 'blue-mariner-100'
  | 'blue-mariner-200'
  | 'blue-mariner-300'
  | 'blue-mariner-400'
  | 'blue-mariner-500'
  | 'blue-mariner-700'
  | 'blue-mariner-800'
  | 'blue-mariner-900'
  | 'blue-mariner-950'
  | 'gray-charade-50'
  | 'gray-charade-100'
  | 'gray-charade-200'
  | 'gray-charade-300'
  | 'gray-charade-500'
  | 'gray-charade-600'
  | 'gray-charade-700'
  | 'gray-charade-800'
  | 'gray-charade-900'
  | 'gray-charade-950'
  | 'white-primary'
  | 'blue-tec'
  | 'mitec-blue'
  | 'mitec-green'
  | 'mitec-red'
  | 'mitec-orange'
  | 'black-primary'
  | 'black-light'
  | 'black-tint'
  | 'black-min'
  | 'white-light'
  | 'white-tint'
  | 'white-min'
  | 'neon-primary'
  | 'neon-light'
  | 'neon-tint'
  | 'blue-primary'
  | 'blue-light'
  | 'blue-tint'
  | 'green-primary'
  | 'green-light'
  | 'green-tint'
  | 'purple-primary'
  | 'purple-light'
  | 'purple-tint'
  | 'red-primary'
  | 'red-light'
  | 'red-tint'
  | 'yellow-primary'
  | 'yellow-light'
  | 'yellow-tint'
  | 'teal-primary'
  | 'teal-light'
  | 'teal-tint'
  | 'container-home'
  | 'container-secondary'
  | 'container-button'
  | 'background-main'
  | 'container-home-light'
  | 'container-secondary-light'
  | 'container-button-light'
  | 'background-main-light'
  | 'container-home-tec'
  | 'container-secondary-tec'
  | 'container-button-tec'
  | 'background-main-tec';
`
      }
    }
  },
  title: {
    control: {
      type: 'text'
    },
    description: 'Sets the title of the card.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string (required)'
      }
    }
  },
  subtitle: {
    control: {
      type: 'text'
    },
    description: 'Sets the card subtitle.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl(),
      type: {
        summary: 'string (optional)'
      }
    }
  }
};
const DBmbDropdownMenuParamDesc = {
  items: {
    control: {
      type: 'object'
    },
    description: `
Sets the data to be displayed in the dropdown menu.

It is an array of objects representing menu items, providing additional actions or navigation options within the card button.

Each object in the array should contain the following properties:

- \`icon\`: (string) The name of the icon displayed next to the menu item text.

- \`text\`: (string) The text label for the menu item.

- \`url\`: (optional, string) The URL to navigate to when the menu item is clicked.

- \`target\`: (optional, string) Specifies where to display the linked URL (e.g., \`_self\`, \`_blank\`).

- \`action\`: (optional, function) A custom function executed when the menu item is clicked${ON_CLICK_DESCRIPTION}. This is useful for triggering specific behaviors or events.
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'IDropdownItem[]',
        detail: `
IDropdownItem {
idItem?: string;
icon: string;
text: string;
selectedText?: string;
value?: string;
url?: string;
target?: IBmbTargetLink;
action?: () => void;
}

IBmbTargetLink = '_blank' | '_parent' | '_self' | '_top';
        `
      },
      defaultValue: {
        summary: '[]'
      }
    }
  }
};
const DBmbTooltipParamDesc = {
  text: {
    control: {
      type: 'text'
    },
    description: 'Sets the detail or description to show as a tooltip.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  align: {
    control: {
      type: 'radio'
    },
    options: ['above', 'below', 'left', 'right'],
    description: 'Sets the position of the tooltip.',
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('right'),
      type: {
        summary: 'IBmbAlignTooltip',
        detail: "IBmbAlignTooltip = 'above' | 'below' | 'left' | 'right'"
      }
    }
  },
  justify: {
    control: {
      type: 'radio'
    },
    options: ['centered', 'before', 'after'],
    description: "Sets the justification with reference to the icon's tooltip.",
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('after'),
      type: {
        summary: 'IBmbJustifyTooltip',
        detail: "IBmbJustifyTooltip = 'centered' | 'before' | 'after'"
      }
    }
  }
};
const DBmbInputParamDesc = {
  inputId: {
    description: `

${UNIQUE_IDENTIFY_DESCRIPTION} the field.

This property is used to link the label to the field through the ***for*** attribute, improving accessibility and usability.
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'randomUUID'
      }
    },
    control: {
      type: 'text'
    }
  },
  name: {
    control: {
      type: 'text'
    },
    description: `
Sets the field name.

<br/>
${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.important}

This property is essential for correct behavior of the the \`FormControl\`.

<br/>
If no name is assigned, a name will be added using \`window.crypto.randomUUID()\`
`,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'randomUUID'
      }
    }
  },
  value: {
    control: {
      type: 'text'
    },
    description: `
Sets the value of the field.

This is the value that is taken when the form is submitted.

${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.important}

The value will not be necessary to define a value on the \`FormControl\` instance, as long as this properties is correctly assigned of this field .

${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note}

The configuration implemented in the \`FormControl\` object will always be prioritized.

    formGroup: FormGroup = new FormGroup({
     fieldName: new FormControl('test'),
    });
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  label: {
    control: {
      type: 'text'
    },
    description: `
${getLabelDescription('above')}
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  checkboxLabel: getCheckboxOrRadialLabel('checkbox'),
  radialLabel: getCheckboxOrRadialLabel('radial'),
  checkboxLabelPosition: getLabelPosition('checkbox'),
  radialLabelPosition: getLabelPosition('radial'),
  tooltip: {
    control: {
      type: 'text'
    },
    description: DBmbTooltipParamDesc.text.description.concat(' It will appear above the field.'),
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  tooltipPosition: {
    control: {
      type: 'object'
    },
    description: `
Sets the position of the tooltip.

Properties:
- \`align\`: ${DBmbTooltipParamDesc.align.description}
- \`justify\`: ${DBmbTooltipParamDesc.justify.description}
`,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl("{ align:'above', justify:'before' }"),
      type: {
        summary: 'IBmbInputTooltipPosition',
        detail: `
IBmbInputTooltipPosition {
  align: IBmbAlignTooltip;
  justify: IBmbJustifyTooltip;
}

${DBmbTooltipParamDesc.align.table.type.detail}

${DBmbTooltipParamDesc.justify.table.type.detail}
        `
      }
    }
  },
  placeholder: {
    control: {
      type: 'text'
    },
    description: 'Sets the text to be displayed as a placeholder within the field.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  icon: {
    control: {
      type: 'text'
    },
    description: ICON_DESCRIPTION.concat('<br/><br/>The icon to display within the field.'),
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  disabled: {
    control: {
      type: 'boolean'
    },
    description: `
${DISABLE_DESCRIPTION.replace('element', 'field')}

It will not be necessary to define disabled property on the \`FormControl\` instance, as long as this properties is correctly assigned of this field .
    `,
    table: {
      category: 'Properties',
      defaultValue: getDefaultValueControl('false'),
      type: {
        summary: 'boolean'
      }
    }
  },
  isRequired: {
    control: {
      type: 'boolean'
    },
    description: `
Sets the field as required when true.

Specifies whether the field must be filled out before submitting the form.

This is commonly used to ensure that users do not skip mandatory choices in forms, enhancing data integrity and user interaction compliance.
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'boolean'
      },
      defaultValue: getDefaultValueControl('false')
    }
  },
  isClearable: {
    control: {
      type: 'boolean'
    },
    description: 'Sets the skill to clear the contents of this field when true',
    table: {
      category: 'Properties',
      type: {
        summary: 'boolean'
      },
      defaultValue: getDefaultValueControl('false')
    }
  },
  helperMessage: {
    control: {
      type: 'text'
    },
    description: 'Sets the text to be displayed as a supporting message below the field.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  errorMessage: {
    description: `
Sets the error message instance or required field message to display below the field when there is an error in it.

For fields that only require mandatory field validation, it is sufficient to assign a string.

In case the field needs more than one validation per field, it is recommended to instantiate the error specification.

Validations supported in instantiation:

- **required**: corresponds to the validation assigned to the \`isRequired\` property
- **min**: corresponds to the validation assigned to the \`min\` property, used for numeric fields
- **max**: corresponds to the validation assigned to the \`max\` property, used for numeric fields
- **minLength**: corresponds to the validation assigned to the \`minlength\` property, used for text and textarea fields
- **maxLength**: corresponds to the validation assigned to the \`maxlength\` property, used for text and textarea fields
- **pattern**: corresponds to the validation assigned to the \`pattern\` property
- **jsonFormat**: corresponds to the validation assigned to the \`jsonFormat\` property, used for textarea fields
- **customValidation**: corresponds to the validation assigned to the \`customValidation\` property

<br/>
${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note}

Default error messages will be displayed if this property is not assigned correctly.
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'string or IBmbInputError',
        detail: `
IBmbInputError {
  required?: string;
  min?: string;
  max?: string;
  minLength?: string;
  maxLength?: string;
  pattern?: string;
  jsonFormat?: string;
  customValidation?: string;
}
          `
      },
      defaultValue: getDefaultValueControl()
    }
  },
  customValidation: {
    control: {
      type: 'object'
    },
    description: `
Sets custom validator function to the field.

Example of a \`ValidatorFn\`
    handleCustomValidator(): ValidatorFn {
      return (control: AbstractControl): ValidationErrors | null => {
        const { value } = control;
        if (!value) return null;

        if (value === 'value example') return { customValidation: true };

        return null;
      };
    }
`,
    table: {
      category: 'Properties',
      type: {
        summary: 'ValidatorFn'
      }
    }
  },
  ariaDescribedBy: {
    control: {
      type: 'text'
    },
    description: 'Provides additional descriptive text for the field, enhancing accessibility by linking the field to a descriptive element by ID.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  ariaLabel: {
    control: {
      type: 'text'
    },
    description: 'Defines a string that labels the field for accessibility purposes, which can be used when a visible label text is not present.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  ariaLabelledBy: {
    control: {
      type: 'text'
    },
    description: 'Identifies the element(s) that labels the field for accessibility purposes, providing a reference to the IDs of the elements that serve as the field label.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl()
    }
  },
  control: {
    control: {
      type: 'object'
    },
    description: getControlDescription(true),
    table: {
      category: 'Properties',
      type: {
        summary: 'FormControl'
      },
      defaultValue: {
        summary: null
      }
    }
  },
  showError: {
    control: false,
    description: `
${(0,_utils__WEBPACK_IMPORTED_MODULE_0__.getDeprecatedDesc)({
      type: 'property'
    })}

**Clarification:**

This property is not required or used because error validation is performed by \`FomControl\`.

Below is a snippet of the **TypeScript example** that performs automatic validation and marks the field as visited:
    ...
    updateErrorState(): void {
      ...
      control.markAsTouched();
      control.updateValueAndValidity();
      ...
    `,
    table: {
      category: 'Deprecated',
      defaultValue: false
    }
  },
  id: {
    control: false,
    description: `
${(0,_utils__WEBPACK_IMPORTED_MODULE_0__.getDeprecatedDesc)({
      type: 'property'
    })}

**Clarification:**

Adding the id using a property with the same name affects the operation of the field.
    `,
    table: {
      category: 'Deprecated',
      defaultValue: false
    }
  },
  dateFormat: {
    control: {
      type: 'text'
    },
    description: 'Sets the date format allowed for validation of entered data.',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl('dd/MM/yyyy')
    }
  },
  invalidFormatErrorMessage: {
    control: {
      type: 'text'
    },
    description: 'Sets an error message for format field validation',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl('Por favor ingresa la fecha con formato [`dateFormat`]}')
    }
  },
  requiredFieldErrorMessage: {
    control: {
      type: 'text'
    },
    description: 'Sets an error message for required field validation',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: getDefaultValueControl('Por favor ingresa la fecha de [`label`]')
    }
  },
  onKeyDown: getOnEventParam((0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'onKeyDown', 'KeyboardEvent'), 'keyDown')
};
const DBmbLayoutParamDesc = {
  gapSize: {
    control: {
      type: 'select'
    },
    options: ['xs', 's', 'm', 'l', 'xl', 'none', 'auto'],
    table: {
      type: {
        summary: 'string',
        detail: `SizeNames = 'xs' | 's' | 'm' | 'l' | 'xl' | 'none' | 'auto'`
      },
      category: 'Properties',
      defaultValue: getDefaultValueControl('m')
    },
    description: 'Sets the size of the space (gap) between elements.'
  },
  justify: {
    control: {
      type: 'select'
    },
    options: ['center', 'end', 'start', 'stretch', 'spaceAround', 'spaceBetween', 'spaceEvenly'],
    table: {
      type: {
        summary: 'string',
        detail: `IJustifyOptions =
  | 'center'
  | 'end'
  | 'start'
  | 'stretch'
  | 'spaceAround'
  | 'spaceBetween'
  | 'spaceEvenly';`
      },
      category: 'Properties',
      defaultValue: getDefaultValueControl('start')
    },
    description: 'Sets the justification (justify-content) of elements.'
  },
  alignItems: {
    control: {
      type: 'select'
    },
    options: ['center', 'end', 'start', 'stretch'],
    table: {
      type: {
        summary: 'string',
        detail: `IAlignItemsOptions = 'center' | 'end' | 'start' | 'stretch'`
      },
      category: 'Properties',
      defaultValue: getDefaultValueControl('start')
    },
    description: 'Sets the alignment (align-items) of elements.'
  }
};
const DBmbStepProgressBar = {
  activeStep: {
    control: {
      type: 'number'
    },
    description: `Sets the step that is active.
    ${(0,_utils__WEBPACK_IMPORTED_MODULE_0__.getAlertBlockquote)(`***activeStep = 0*** is for **step number** 1.
      Therefore, should always be assigned to the \`activeStep\` the **step number** - 1.`, {
      title: _utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.warning,
      blockquoteType: _utils__WEBPACK_IMPORTED_MODULE_0__.BlockquoteType.warning
    })}
    `,
    table: {
      category: 'Properties',
      type: {
        summary: 'number'
      },
      defaultValue: {
        summary: '0'
      }
    }
  },
  totalSteps: {
    control: {
      type: 'number'
    },
    description: 'Sets the number of steps that the counter will show.',
    table: {
      category: 'Properties',
      type: {
        summary: 'number'
      }
    }
  },
  size: {
    control: {
      type: 'radio'
    },
    options: ['small', 'medium', 'default'],
    description: `Sets the size of the steps.<br/><br/> ${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.warning} This property only works when the ***type*** is ***horizontal***.`,
    table: {
      category: 'Properties',
      type: {
        summary: 'select'
      },
      defaultValue: {
        summary: 'default'
      }
    }
  },
  freeze: {
    control: {
      type: 'boolean'
    },
    description: 'Freezes the state of progress steps.',
    table: {
      category: 'Properties',
      type: {
        summary: 'boolean'
      },
      defaultValue: {
        summary: 'false'
      }
    }
  },
  type: {
    control: {
      type: 'radio'
    },
    options: ['horizontal', 'vertical'],
    description: 'Changes the direction of the step progress bar, could be horizontal or vertical',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'horizontal'
      }
    }
  },
  labelSteps: {
    control: {
      type: 'array'
    },
    description: 'Sets the label for each step',
    table: {
      category: 'Properties',
      type: {
        summary: 'string[]'
      },
      defaultValue: {
        summary: '[]'
      }
    }
  },
  labelComplete: {
    control: {
      type: 'text'
    },
    description: 'Sets the label for complete steps',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'Completo'
      }
    }
  },
  labelIncomplete: {
    control: {
      type: 'text'
    },
    description: 'Sets the label for incomplete steps',
    table: {
      category: 'Properties',
      type: {
        summary: 'string'
      },
      defaultValue: {
        summary: 'Pendiente'
      }
    }
  },
  onStepPress: getOnEventParam((0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'onStepPress', 'number'), `with the index of the step when a step is pressed.`, 'other'),
  onStepPanelPress: getOnEventParam((0,_utils__WEBPACK_IMPORTED_MODULE_0__.getOnEvent)('', 'onStepPanelPress', 'number'), `with the index when a step panel is pressed.`, 'other')
};
const getPropertyForType = (isCounter = true, isSimple = false) => `
<br/><br/>This property is displayed for the property type:
${isSimple ? '- simple' : ''}
${isCounter ? '- counter' : ''}
- container
`;
const DBmbProgressBar = {
  textFormat: {
    control: {
      type: 'function'
    },
    description: `
  Sets the text format function to show the percentage in the progress bar.
  ${getPropertyForType()}
  The function receives two parameters: the current value and the total value, and should return a formatted string.

  If not set, it defaults to showing the value as *"value/total"*.

  ${_utils__WEBPACK_IMPORTED_MODULE_0__.RELEVANT_TITLE.note} Avoid return HTML code, whether HTML code will be parsed.`,
    table: {
      category: 'Properties',
      type: {
        summary: '(value: string, total: string) => string',
        detail: 'textFormat: (value: string, total: string) => `$${value}/$${total}MXN`'
      },
      defaultValue: getDefaultValueControl('value => value')
    }
  }
};

/***/ }

});
//# sourceMappingURL=projects_ds-ng_src_lib_utils_doc_parameterDescriptions_ts.31a99488309436b6599c.hot-update.js.map