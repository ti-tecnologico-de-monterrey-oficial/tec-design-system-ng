import { Meta } from '@storybook/addon-docs/blocks';

import * as studentStory from './bmb-identity-spectrum.stories';
import * as collaboratorStory from './bmb-identity-spectrum-collaborator.stories';
import { ListTemplate } from '../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../utils/doc/utils';

<Meta title="Organisms/Identity spectrum" />
<ListTemplate
  content={[studentStory, collaboratorStory]}
  context={{
    [getFormatName(
      collaboratorStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
  }}
/>
