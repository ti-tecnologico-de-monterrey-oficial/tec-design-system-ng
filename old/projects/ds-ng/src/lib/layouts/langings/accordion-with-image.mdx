import { Meta } from '@storybook/addon-docs/blocks';

import * as mainStory from '../bmb_landing_accordion_tabs_image.stories';
import * as subStory from '../bmb_landing_caja_ahorro.stories';
import { ListTemplate } from '../../../DocComponents/ListTemplate.mdx';
import { getFormatName } from '../../utils/doc/utils';

<Meta of={mainStory} />
<ListTemplate
  main={mainStory}
  content={[subStory]}
  context={{
    [getFormatName(
      subStory.default.title,
      /(\/)|( )/g,
      '_',
    ).toLocaleLowerCase()]: {
      showStories: false,
      showControls: false,
    },
  }}
/>
